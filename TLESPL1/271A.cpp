#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int y[4]= {0};
    int freq[10] = {0};

    for(int i=0; i<4; i++)
    cin >> y[i];

    boolean isUnique (int arr[])
    {
      for (int i=0;i<4;i++){
        freq[arr[i]]++;
      }
    }
    return 0;
}

